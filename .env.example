# Public origin of the independently deployed FastAPI service. Do not include a trailing slash.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Keep true for the included demo dataset. Set false when the FastAPI endpoints are ready.
NEXT_PUBLIC_USE_MOCK_API=true
